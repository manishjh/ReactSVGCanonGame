// const initialState = {
//     message: "It's easy to integrate React and Redux, isn't it?",
// };

const initialState = "hakunaMatata";

export default function (state = initialState, action) {
    return state;
}


